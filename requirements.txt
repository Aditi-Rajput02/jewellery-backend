fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv>=1.0.0
Pillow>=10.4.0
requests>=2.31.0
pymongo>=4.6.0
openai>=2.0.0

# ── RAG Pipeline ──────────────────────────────────────────────────────────────
# ChromaDB: persistent vector store for RAG transcripts
# Embeddings: OpenAI text-embedding-3-small (1536-dim) via API — no local model
# NOTE: render.yaml build command runs `rm -rf rag_chroma_db` before install
#       to clear any old 512-dim CLIP embeddings from previous deploys.
chromadb>=1.0.0
